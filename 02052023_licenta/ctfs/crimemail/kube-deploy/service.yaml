apiVersion: v1
kind: Service
metadata:
  name: ctf-crimemail-service
  namespace: ctfspace
spec:
  selector:
    app: ctf-crimemail
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
