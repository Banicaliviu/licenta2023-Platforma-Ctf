apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  #annotations:
    #cert-manager.io/cluster-issuer: test-ca-cluster-issuer
  name: flask-ingress
  namespace: ctfspace
spec:
  rules:
  - http:
     paths:
     - pathType: Prefix
       path: /
       backend:
        service:
         name: flask-service
         port:
          number: 8080
    #- host: flask.192.168.49.2.nip.io
  # tls:
  # - hosts:
  #   - flask.192.168.49.2.nip.io
  #   secretName: myingress-cert
