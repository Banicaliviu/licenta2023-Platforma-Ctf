apiVersion: v1
kind: Service
metadata:
  name: {{ include "ctfchart.fullname" . }}
  labels:
    {{- include "ctfchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ctfchart.selectorLabels" . | nindent 4 }}
